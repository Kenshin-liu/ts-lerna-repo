{
  "name": "ts-lernarepo",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "dev": "tsnd src/index",
    "publish": "lerna publish"
  },
  "devDependencies": {
    "lerna": "^3.19.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.4.4"
  }
}
